function add(){
    var firstOp, secondOp, result;
  firstOp =  document.getElementById('input1').value;
    secondOp =  document.getElementById('input2').value;
    result = parseInt(firstOp) + parseInt(secondOp);
     window.alert(result);
   }
     function add1(){
       var firstOp, secondOp, result;
     firstOp =  document.getElementById('input1').value;
       secondOp =  document.getElementById('input2').value;
       result = parseInt(firstOp) - parseInt(secondOp);
        window.alert(result);
      }
        function add2(){
          var firstOp, secondOp, result;
        firstOp =  document.getElementById('input1').value;
          secondOp =  document.getElementById('input2').value;
          result = parseInt(firstOp) * parseInt(secondOp);
           window.alert(result);
         }
           function add3(){
             var firstOp, secondOp, result;
           firstOp =  document.getElementById('input1').value;
             secondOp =  document.getElementById('input2').value;
             result = parseInt(secondOp) / parseInt(firstOp);
              window.alert(result);
   }
